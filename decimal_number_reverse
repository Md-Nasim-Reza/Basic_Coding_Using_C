#include<stdio.h>
main(){
int number, sum1, sum=0;
printf("please enter the number:");
scanf("%d", &number);

    while(number % 10 != 0){
     sum1 = number % 10;
     sum = sum + sum1;
    number = number/10;
    }
printf("%d", sum);
}
