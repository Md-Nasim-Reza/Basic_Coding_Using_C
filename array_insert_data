
#include<stdio.h>
main(){
    int arr[100],i,n,position,valu;
    scanf("%d", &n);
    for(i=0; i<n; i++)
        scanf("%d", &arr[i]);
    printf("Enter the position number\n");
        scanf("%d", &position);
    printf("Enter the valu\n");
        scanf("%d", &valu);
    for(i=n-1; i>=position; i--)
        arr[i+1]=arr[i];
    arr[position]=valu;
    printf("valu is output\n");
    printf("%d\n", siz);
    for(i=0; i<=n; i++)
        printf("%d ", arr[i]);
}
