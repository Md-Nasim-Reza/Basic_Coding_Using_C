#include<stdio.h>
#include<string.h>
main()
{
    char str[100];
    int len, x=0,i;
    gets(str);
    len = strlen(str);
    for(i=0; i<len; i++){
        if(str[i] !=' ')
            if(str[i]>='0' && str[i]<='9')
                x = (x*10)+str[i]-'0';
    }
    if(str)
    printf("%d\n", x);


}
