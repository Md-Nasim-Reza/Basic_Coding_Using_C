
#include<stdio.h>
main(){
    int i,j,n,x,arr[100010]={0};
    scanf("%d", &n);
    for(i=0; i<n; i++){
        scanf("%d",&x);
        arr[x]++;
    }
    for(i=0; i<=n; i++){
        if(arr[i]!=0)
        printf("%d - %d\n", i,arr[i]);
    }
}
