
#include<stdio.h>
main(){
int i=1, number, even=0, odd=0;
printf("enter the number:");
scanf("%d", &number);
    while(i<=number){
    if(i%2==0)
    even++;
    else
        odd++;
    i++;
    }
printf("even= %d", even);
printf("\nodd= %d", odd);

}
