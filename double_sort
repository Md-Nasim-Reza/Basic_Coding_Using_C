#include<stdio.h>
main(){
    int i,j,n,arr[100010]={0},x;
    scanf("%d", &n);
    for(i=0; i<n; i++){
        scanf("%d", &x);
        arr[x]++;
    }
    for(i=0; i<=n; i++){
        if(arr[i] != 0)
            for(j=0; j<arr[i]; j++)
                printf("%d ", i);

    }


}
