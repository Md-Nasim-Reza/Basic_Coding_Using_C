#include<stdio.h>
main(){
    int n,count=0,sum=0,a;
    scanf("%d", &n);
    while(n){
        a=n%10;
        sum=sum+a;
        n=n/10;
        count++;
    }
    printf("DIGET COUNT = %d\n", count);
    printf("DIGET SUMATION = %d\n", sum);

}
